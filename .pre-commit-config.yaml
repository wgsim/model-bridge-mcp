repos:
  - repo: local
    hooks:
      - id: unit-tests
        name: unit-tests
        language: system
        entry: conda run -n model-bridge-mcp_dev bash -lc 'PYTHONPATH=src pytest -q tests/unit'
        pass_filenames: false
        always_run: true
